<h1>Shopping Lists</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% lists.each do |list| %>
  <tr>
    <td><%= list.name %></td>
    <td><%= link_to 'Show', list, class: "button tiny secondary" %></td>
    <td><%= link_to 'Edit', edit_list_path(list), class: "button tiny secondary" %></td>
    <td><%= link_to 'Delete List', list, class: "button tiny secondary" , method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Add A New List', new_list_path %>
